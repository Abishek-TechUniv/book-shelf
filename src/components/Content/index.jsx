import React from 'react';
import './Content.css';


const Content = (booksArr) => {
  const itemsArr = notesArr.map(no);
  return (
    <div className="Content-container">
  Hello
    </div>
  );
};

export default Content;
